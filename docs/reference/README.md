# Design references

- The build prompt's island composition reference image was not delivered
  as a file, so nothing ships here. If it turns up, place it at
  docs/reference/bain-beach.png. It stays design documentation only and is
  never used as a runtime background.
- The lawn banner ("BAIN & COMPANY" pixel barrier) was supplied in
  conversation as an image and recreated in code at world scale in
  `game/world/paint.ts` (paintBanner). The original was not retained.
